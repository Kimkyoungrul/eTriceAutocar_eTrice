/**
 * @author generated by eTrice
 *
 * Source File of ActorClass TrafficLightControllerTestHarness
 *
 */

#include "TrafficLightControllerTestHarness.h"

#include "modelbase/etActor.h"
#include "debugging/etLogger.h"
#include "debugging/etMSCLogger.h"
#include "etUnit/etUnit.h"
#include "base/etMemory.h"

#include "room/basic/service/timing/PTimer.h"
#include "TrafficLight/PTrafficLightController.h"
#include "TrafficLight/PTrafficLightInterface.h"

#include "TrafficLightControllerTestHarness_Utils.h"


/* interface item IDs */
enum interface_items {
	IFITEM_fct = 1,
	IFITEM_tlInterface = 2,
	IFITEM_to = 3
};

/* state IDs */
enum state_ids {
	NO_STATE = 0,
	STATE_TOP = 1,
	STATE_SetStandby = 2,
	STATE_InStandby = 3,
	STATE_SetStart = 4,
	STATE_InRunning = 5,
	STATE_CarGreen = 6,
	STATE_PressButton = 7,
	STATE_CarGreenAgain = 8,
	STATE_CarYellow = 9,
	STATE_SetStop = 10,
	STATE_Done = 11,
	STATE_MAX = 12
};

/* transition chains */
enum ChainIDs {
	CHAIN_TRANS_INITIAL_TO__SetStandby = 1,
	CHAIN_TRANS_tr0_FROM_SetStandby_TO_InStandby_BY_doneStandbyfct = 2,
	CHAIN_TRANS_tr1_FROM_InStandby_TO_SetStart_BY_setCarYellowtlInterface = 3,
	CHAIN_TRANS_tr2_FROM_SetStart_TO_InRunning_BY_doneStartfct = 4,
	CHAIN_TRANS_tr3_FROM_InRunning_TO_CarGreen_BY_setCarGreentlInterface = 5,
	CHAIN_TRANS_tr4_FROM_CarGreen_TO_PressButton_BY_timeoutto = 6,
	CHAIN_TRANS_tr5_FROM_PressButton_TO_CarGreenAgain_BY_setCarGreentlInterface = 7,
	CHAIN_TRANS_tr6_FROM_CarGreenAgain_TO_CarYellow_BY_setCarYellowtlInterface = 8,
	CHAIN_TRANS_tr7_FROM_CarYellow_TO_SetStop_BY_setPedRedtlInterface = 9,
	CHAIN_TRANS_tr8_FROM_SetStop_TO_Done_BY_doneStopfct = 10
};

/* triggers */
enum triggers {
	POLLING = 0,
	TRIG_fct__doneStandby = IFITEM_fct + EVT_SHIFT*PTrafficLightController_OUT_doneStandby,
	TRIG_fct__doneStart = IFITEM_fct + EVT_SHIFT*PTrafficLightController_OUT_doneStart,
	TRIG_fct__doneStop = IFITEM_fct + EVT_SHIFT*PTrafficLightController_OUT_doneStop,
	TRIG_tlInterface__connect = IFITEM_tlInterface + EVT_SHIFT*PTrafficLightInterface_IN_connect,
	TRIG_tlInterface__setCarRed = IFITEM_tlInterface + EVT_SHIFT*PTrafficLightInterface_IN_setCarRed,
	TRIG_tlInterface__setCarYellow = IFITEM_tlInterface + EVT_SHIFT*PTrafficLightInterface_IN_setCarYellow,
	TRIG_tlInterface__setCarGreen = IFITEM_tlInterface + EVT_SHIFT*PTrafficLightInterface_IN_setCarGreen,
	TRIG_tlInterface__setPedRed = IFITEM_tlInterface + EVT_SHIFT*PTrafficLightInterface_IN_setPedRed,
	TRIG_tlInterface__setPedGreen = IFITEM_tlInterface + EVT_SHIFT*PTrafficLightInterface_IN_setPedGreen,
	TRIG_tlInterface__setRequestButtonActive = IFITEM_tlInterface + EVT_SHIFT*PTrafficLightInterface_IN_setRequestButtonActive,
	TRIG_to__timeout = IFITEM_to + EVT_SHIFT*PTimer_OUT_timeout
};

/* state names */
static char* stateStrings[] = {"<no state>","<top>","SetStandby",
"InStandby",
"SetStart",
"InRunning",
"CarGreen",
"PressButton",
"CarGreenAgain",
"CarYellow",
"SetStop",
"Done"
};

static void setState(TrafficLightControllerTestHarness* self, etInt16 new_state) {
	self->state = new_state;
	ET_MSC_LOGGER_CHANGE_STATE(self->constData->instName, stateStrings[new_state])
}

static etInt16 getState(TrafficLightControllerTestHarness* self) {
	return self->state;
}

/* Entry and Exit Codes */
static void entry_SetStandby(TrafficLightControllerTestHarness* self) {
	fct_standby(0); // dummy port not used
}
static void entry_SetStart(TrafficLightControllerTestHarness* self) {
	fct_start();
}
static void entry_CarGreen(TrafficLightControllerTestHarness* self) {
	to_startTimeout(1*10);
}
static void entry_PressButton(TrafficLightControllerTestHarness* self) {
	tlInterface_pressedRequestButton();
}
static void entry_CarGreenAgain(TrafficLightControllerTestHarness* self) {
	fct_standby(0); // dummy port
}
static void entry_SetStop(TrafficLightControllerTestHarness* self) {
	fct_stop();
}

/* Action Codes */
static void action_TRANS_tr0_FROM_SetStandby_TO_InStandby_BY_doneStandbyfct(TrafficLightControllerTestHarness* self, const InterfaceItemBase* ifitem) {
    counter=0;
}
static void action_TRANS_tr6_FROM_CarGreenAgain_TO_CarYellow_BY_setCarYellowtlInterface(TrafficLightControllerTestHarness* self, const InterfaceItemBase* ifitem, bool status) {
    counter=0;
}

/* State Switch Methods */
/**
 * calls exit codes while exiting from the current state to one of its
 * parent states while remembering the history
 * @param current__et - the current state
 * @param to - the final parent state
 */
static void exitTo(TrafficLightControllerTestHarness* self, etInt16 current__et, etInt16 to) {
	while (current__et!=to) {
		switch (current__et) {
			case STATE_SetStandby:
				self->history[STATE_TOP] = STATE_SetStandby;
				current__et = STATE_TOP;
				break;
			case STATE_InStandby:
				self->history[STATE_TOP] = STATE_InStandby;
				current__et = STATE_TOP;
				break;
			case STATE_SetStart:
				self->history[STATE_TOP] = STATE_SetStart;
				current__et = STATE_TOP;
				break;
			case STATE_InRunning:
				self->history[STATE_TOP] = STATE_InRunning;
				current__et = STATE_TOP;
				break;
			case STATE_CarGreen:
				self->history[STATE_TOP] = STATE_CarGreen;
				current__et = STATE_TOP;
				break;
			case STATE_PressButton:
				self->history[STATE_TOP] = STATE_PressButton;
				current__et = STATE_TOP;
				break;
			case STATE_CarGreenAgain:
				self->history[STATE_TOP] = STATE_CarGreenAgain;
				current__et = STATE_TOP;
				break;
			case STATE_CarYellow:
				self->history[STATE_TOP] = STATE_CarYellow;
				current__et = STATE_TOP;
				break;
			case STATE_SetStop:
				self->history[STATE_TOP] = STATE_SetStop;
				current__et = STATE_TOP;
				break;
			case STATE_Done:
				self->history[STATE_TOP] = STATE_Done;
				current__et = STATE_TOP;
				break;
			default:
				/* should not occur */
				break;
		}
	}
}

/**
 * calls action, entry and exit codes along a transition chain. The generic data are cast to typed data
 * matching the trigger of this chain. The ID of the final state is returned
 * @param chain__et - the chain ID
 * @param generic_data__et - the generic data pointer
 * @return the +/- ID of the final state either with a positive sign, that indicates to execute the state's entry code, or a negative sign vice versa
 */
static etInt16 executeTransitionChain(TrafficLightControllerTestHarness* self, int chain__et, const InterfaceItemBase* ifitem, void* generic_data__et) {
	switch (chain__et) {
		case CHAIN_TRANS_INITIAL_TO__SetStandby:
		{
			return STATE_SetStandby;
		}
		case CHAIN_TRANS_tr0_FROM_SetStandby_TO_InStandby_BY_doneStandbyfct:
		{
			action_TRANS_tr0_FROM_SetStandby_TO_InStandby_BY_doneStandbyfct(self, ifitem);
			return STATE_InStandby;
		}
		case CHAIN_TRANS_tr1_FROM_InStandby_TO_SetStart_BY_setCarYellowtlInterface:
		{
			bool status = *((bool*) generic_data__et);
			return STATE_SetStart;
		}
		case CHAIN_TRANS_tr2_FROM_SetStart_TO_InRunning_BY_doneStartfct:
		{
			return STATE_InRunning;
		}
		case CHAIN_TRANS_tr3_FROM_InRunning_TO_CarGreen_BY_setCarGreentlInterface:
		{
			bool status = *((bool*) generic_data__et);
			return STATE_CarGreen;
		}
		case CHAIN_TRANS_tr4_FROM_CarGreen_TO_PressButton_BY_timeoutto:
		{
			return STATE_PressButton;
		}
		case CHAIN_TRANS_tr5_FROM_PressButton_TO_CarGreenAgain_BY_setCarGreentlInterface:
		{
			bool status = *((bool*) generic_data__et);
			return STATE_CarGreenAgain;
		}
		case CHAIN_TRANS_tr6_FROM_CarGreenAgain_TO_CarYellow_BY_setCarYellowtlInterface:
		{
			bool status = *((bool*) generic_data__et);
			action_TRANS_tr6_FROM_CarGreenAgain_TO_CarYellow_BY_setCarYellowtlInterface(self, ifitem, status);
			return STATE_CarYellow;
		}
		case CHAIN_TRANS_tr7_FROM_CarYellow_TO_SetStop_BY_setPedRedtlInterface:
		{
			bool status = *((bool*) generic_data__et);
			return STATE_SetStop;
		}
		case CHAIN_TRANS_tr8_FROM_SetStop_TO_Done_BY_doneStopfct:
		{
			return STATE_Done;
		}
			default:
				/* should not occur */
				break;
	}
	return NO_STATE;
}

/**
 * calls entry codes while entering a state's history. The ID of the final leaf state is returned
 * @param state__et - the state which is entered
 * @return - the ID of the final leaf state
 */
static etInt16 enterHistory(TrafficLightControllerTestHarness* self, etInt16 state__et) {
	etBool skip_entry__et = ET_FALSE;
	if (state__et >= STATE_MAX) {
		state__et = (etInt16) (state__et - STATE_MAX);
		skip_entry__et = ET_TRUE;
	}
	while (ET_TRUE) {
		switch (state__et) {
			case STATE_SetStandby:
				if (!(skip_entry__et)) entry_SetStandby(self);
				/* in leaf state: return state id */
				return STATE_SetStandby;
			case STATE_InStandby:
				/* in leaf state: return state id */
				return STATE_InStandby;
			case STATE_SetStart:
				if (!(skip_entry__et)) entry_SetStart(self);
				/* in leaf state: return state id */
				return STATE_SetStart;
			case STATE_InRunning:
				/* in leaf state: return state id */
				return STATE_InRunning;
			case STATE_CarGreen:
				if (!(skip_entry__et)) entry_CarGreen(self);
				/* in leaf state: return state id */
				return STATE_CarGreen;
			case STATE_PressButton:
				if (!(skip_entry__et)) entry_PressButton(self);
				/* in leaf state: return state id */
				return STATE_PressButton;
			case STATE_CarGreenAgain:
				if (!(skip_entry__et)) entry_CarGreenAgain(self);
				/* in leaf state: return state id */
				return STATE_CarGreenAgain;
			case STATE_CarYellow:
				/* in leaf state: return state id */
				return STATE_CarYellow;
			case STATE_SetStop:
				if (!(skip_entry__et)) entry_SetStop(self);
				/* in leaf state: return state id */
				return STATE_SetStop;
			case STATE_Done:
				/* in leaf state: return state id */
				return STATE_Done;
			case STATE_TOP:
				state__et = self->history[STATE_TOP];
				break;
			default:
				/* should not occur */
				break;
		}
		skip_entry__et = ET_FALSE;
	}
	/* return NO_STATE; // required by CDT but detected as unreachable by JDT because of while (true) */
}

static void TrafficLightControllerTestHarness_executeInitTransition(TrafficLightControllerTestHarness* self) {
	int chain__et = CHAIN_TRANS_INITIAL_TO__SetStandby;
	etInt16 next__et = executeTransitionChain(self, chain__et, NULL, NULL);
	next__et = enterHistory(self, next__et);
	setState(self, next__et);
}

/* receiveEvent contains the main implementation of the FSM */
static void TrafficLightControllerTestHarness_receiveEventInternal(TrafficLightControllerTestHarness* self, InterfaceItemBase* ifitem, int localId, int evt, void* generic_data__et) {
	int trigger__et = localId + EVT_SHIFT*evt;
	int chain__et = NOT_CAUGHT;
	etInt16 catching_state__et = NO_STATE;
	((void)trigger__et);	/* avoids unused warning */

	if (!handleSystemEvent(ifitem, evt, generic_data__et)) {
		switch (getState(self)) {
		    case STATE_SetStandby:
		        switch(trigger__et) {
		                case TRIG_fct__doneStandby:
		                    {
		                        chain__et = CHAIN_TRANS_tr0_FROM_SetStandby_TO_InStandby_BY_doneStandbyfct;
		                        catching_state__et = STATE_TOP;
		                    }
		                break;
		                default:
		                    /* should not occur */
		                    break;
		        }
		        break;
		    case STATE_InStandby:
		        switch(trigger__et) {
		                case TRIG_tlInterface__setCarYellow:
		                    { bool status = *((bool*) generic_data__et);
		                    if (++counter==5)
		                    {
		                        chain__et = CHAIN_TRANS_tr1_FROM_InStandby_TO_SetStart_BY_setCarYellowtlInterface;
		                        catching_state__et = STATE_TOP;
		                    }
		                    }
		                break;
		                default:
		                    /* should not occur */
		                    break;
		        }
		        break;
		    case STATE_SetStart:
		        switch(trigger__et) {
		                case TRIG_fct__doneStart:
		                    {
		                        chain__et = CHAIN_TRANS_tr2_FROM_SetStart_TO_InRunning_BY_doneStartfct;
		                        catching_state__et = STATE_TOP;
		                    }
		                break;
		                default:
		                    /* should not occur */
		                    break;
		        }
		        break;
		    case STATE_InRunning:
		        switch(trigger__et) {
		                case TRIG_tlInterface__setCarGreen:
		                    { bool status = *((bool*) generic_data__et);
		                    if (status==1)
		                    {
		                        chain__et = CHAIN_TRANS_tr3_FROM_InRunning_TO_CarGreen_BY_setCarGreentlInterface;
		                        catching_state__et = STATE_TOP;
		                    }
		                    }
		                break;
		                default:
		                    /* should not occur */
		                    break;
		        }
		        break;
		    case STATE_CarGreen:
		        switch(trigger__et) {
		                case TRIG_to__timeout:
		                    {
		                        chain__et = CHAIN_TRANS_tr4_FROM_CarGreen_TO_PressButton_BY_timeoutto;
		                        catching_state__et = STATE_TOP;
		                    }
		                break;
		                default:
		                    /* should not occur */
		                    break;
		        }
		        break;
		    case STATE_PressButton:
		        switch(trigger__et) {
		                case TRIG_tlInterface__setCarGreen:
		                    { bool status = *((bool*) generic_data__et);
		                    if (status==1)
		                    {
		                        chain__et = CHAIN_TRANS_tr5_FROM_PressButton_TO_CarGreenAgain_BY_setCarGreentlInterface;
		                        catching_state__et = STATE_TOP;
		                    }
		                    }
		                break;
		                default:
		                    /* should not occur */
		                    break;
		        }
		        break;
		    case STATE_CarGreenAgain:
		        switch(trigger__et) {
		                case TRIG_tlInterface__setCarYellow:
		                    { bool status = *((bool*) generic_data__et);
		                    if (status==1)
		                    {
		                        chain__et = CHAIN_TRANS_tr6_FROM_CarGreenAgain_TO_CarYellow_BY_setCarYellowtlInterface;
		                        catching_state__et = STATE_TOP;
		                    }
		                    }
		                break;
		                default:
		                    /* should not occur */
		                    break;
		        }
		        break;
		    case STATE_CarYellow:
		        switch(trigger__et) {
		                case TRIG_tlInterface__setPedRed:
		                    { bool status = *((bool*) generic_data__et);
		                    if (++counter==5)
		                    {
		                        chain__et = CHAIN_TRANS_tr7_FROM_CarYellow_TO_SetStop_BY_setPedRedtlInterface;
		                        catching_state__et = STATE_TOP;
		                    }
		                    }
		                break;
		                default:
		                    /* should not occur */
		                    break;
		        }
		        break;
		    case STATE_SetStop:
		        switch(trigger__et) {
		                case TRIG_fct__doneStop:
		                    {
		                        chain__et = CHAIN_TRANS_tr8_FROM_SetStop_TO_Done_BY_doneStopfct;
		                        catching_state__et = STATE_TOP;
		                    }
		                break;
		                default:
		                    /* should not occur */
		                    break;
		        }
		        break;
		    case STATE_Done:
		        break;
		    default:
		        /* should not occur */
		        break;
		}
	}
	if (chain__et != NOT_CAUGHT) {
		exitTo(self, getState(self), catching_state__et);
		{
			etInt16 next__et = executeTransitionChain(self, chain__et, ifitem, generic_data__et);
			next__et = enterHistory(self, next__et);
			setState(self, next__et);
		}
	}
}
static void TrafficLightControllerTestHarness_receiveEvent(TrafficLightControllerTestHarness* self, InterfaceItemBase* ifitem, int evt, void* generic_data__et) {
	int localId = (ifitem==NULL)? 0 : ifitem->localId;
	TrafficLightControllerTestHarness_receiveEventInternal(self, ifitem, localId, evt, generic_data__et);
}

void TrafficLightControllerTestHarness_init(TrafficLightControllerTestHarness* self){
	ET_MSC_LOGGER_SYNC_ENTRY("TrafficLightControllerTestHarness", "init")
	self->state = STATE_TOP;
	{
		int i;
		for (i=0; i<TRAFFICLIGHTCONTROLLERTESTHARNESS_HISTORY_SIZE; ++i)
			self->history[i] = NO_STATE;
	}
	TrafficLightControllerTestHarness_executeInitTransition(self);
	ET_MSC_LOGGER_SYNC_EXIT
}


void TrafficLightControllerTestHarness_receiveMessage(void* self, const void* ifitem, const etMessage* msg){
	ET_MSC_LOGGER_SYNC_ENTRY("TrafficLightControllerTestHarness", "_receiveMessage")
	TrafficLightControllerTestHarness_receiveEvent(self, (etPort*)ifitem, msg->evtID, (void*)(((char*)msg)+MEM_CEIL(sizeof(etMessage))));

	ET_MSC_LOGGER_SYNC_EXIT
}


/*--------------------- user constructor/destructor ---------------------*/

/*--------------------- operations ---------------------*/

