/**
 * @author generated by eTrice
 *
 * Header File of ActorClass Controller
 *
 */

#ifndef _TEMPLATEMODEL_CONTROLLER_H_
#define _TEMPLATEMODEL_CONTROLLER_H_

#include "etDatatypes.h"
#include "messaging/etMessage.h"

/* include all referenced room classes */
#include "TemplateModel/PSendData.h"


typedef struct Controller Controller;

/* const part of ActorClass (ROM) */
typedef struct Controller_const {
	const char* instName;

	/* simple ports */
	const PSendDataPort dataPortB;

	/* data receive ports */

	/* saps */

	/* replicated ports */

	/* services */
} Controller_const;


/* variable part of ActorClass (RAM) */
struct Controller {
	const Controller_const* const constData;

	/* data send ports */

	/*--------------------- attributes ---------------------*/
	 char str[32]
	;

};

void Controller_init(Controller* self);

void Controller_receiveMessage(void* self, const void* ifitem, const etMessage* msg);


/*--------------------- user constructor/destructor ---------------------*/

/*--------------------- operations ---------------------*/


#endif /* _TEMPLATEMODEL_CONTROLLER_H_ */

